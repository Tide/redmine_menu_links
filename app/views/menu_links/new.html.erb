<h2><%= link_to l(:label_menu_links), :controller => 'menu_links', :action => 'index' %> &#187; <%=l(:label_menu_link_new)%></h2>

<%= labelled_form_for :menu_link, @menu_link, :url => { :controller => 'menu_links', :action => 'create' } do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p><%= f.submit l(:button_create) %></p>
<% end %>
